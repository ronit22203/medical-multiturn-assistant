# Application / Streamlit UI (RunPod shared env with vLLM)
# Keep Streamlit below 1.36 so it can coexist with fastapi<0.113 / starlette<0.38.
pydantic>=2.7.0,<3
pyyaml>=6.0.1
streamlit>=1.35.0,<1.36.0
openai>=1.30.0
datasets>=2.19.0
